services:
  - type: web
    name: events-data-aggregator
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:8000 --timeout 600 app:app"
    healthCheckPath: /api-check
    plan: free